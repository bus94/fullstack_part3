-- 오라클 developer 프로그램에서 테이블 생성(create), 삭제(drop)는 자동으로 커밋(commit) 된다.
-- 문제는 오라클 developer 프로그램에서 데이터를 추가(insert), 삭제(delete), 수정(update) 할 땐 무조건 commit을 해야한다.

-- 자바에서 보내는 명령문들은 자동으로 커밋된다.
-- 자바에서는 오토커밋이라는 기능이 활성화 되어있기 때문에 자동으로 오라클 DB에 적용되는 것이다.

